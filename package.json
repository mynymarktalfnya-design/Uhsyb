{
  "name": "workspace",
  "version": "0.0.0",
  "main": "desktop/main.cjs",
  "description": "ميني ماركت الفنية - تطبيق نقطة البيع وإدارة المتجر",
  "author": "ميني ماركت الفنية",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "desktop:prepare": "pnpm --filter market-frontend run build && powershell -NoProfile -ExecutionPolicy Bypass -File packaging/windows/build-runtime.ps1",
    "desktop:dist": "pnpm run desktop:prepare && electron-builder --win nsis --x64",
    "desktop:dir": "pnpm run desktop:prepare && electron-builder --dir --win --x64"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.4",
    "typescript": "~5.9.3",
    "electron": "^38.1.0",
    "electron-builder": "^26.0.12"
  }
}
